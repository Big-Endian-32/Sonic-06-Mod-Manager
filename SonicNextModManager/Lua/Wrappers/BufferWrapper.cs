namespace SonicNextModManager.Lua.Wrappers
{
    public class BufferWrapper
    {
        private byte[] _buffer;

        public BufferWrapper() { }

        public BufferWrapper(byte[] in_buffer)
        {
            _buffer = in_buffer;
        }

        // TODO: add buffer helper functions.
    }
}
